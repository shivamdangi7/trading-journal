{
  "name": "trading-journal",
  "version": "1.0.0",
  "description": "Trading Journal & Watchlist Standalone Native & Web App",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/",
    "cap:sync": "npm run build && npx cap copy android",
    "cap:android": "npm run cap:sync && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
